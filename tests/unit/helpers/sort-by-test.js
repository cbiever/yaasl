
import { sortBy } from 'yaasl/helpers/sort-by';
import { module, test } from 'qunit';

module('Unit | Helper | sort by');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortBy([42]);
  assert.ok(result);
});

