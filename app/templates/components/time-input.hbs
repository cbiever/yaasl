{{paper-input
  placeholder=(t placeholder)
  type=text
  value=editBuffer
  onChange=(action (mut editBuffer))
  onBlur=(action 'updateTime')
  required=true
  errors=errors
  errorMessages=(hash
    required=missing
  )
  disabled=disabled
  class=class
}}
